{
    "version": 2,
    "framework": null,
    "installCommand": "pnpm install",
    "buildCommand": "pnpm db:generate && pnpm build",
    "outputDirectory": "dist",
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/server.js"
        }
    ]
}