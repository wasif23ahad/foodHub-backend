{
    "version": 2,
    "installCommand": "pnpm install && pnpm db:generate",
    "buildCommand": "pnpm build",
    "builds": [
        {
            "src": "src/server.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "src/server.ts"
        }
    ]
}