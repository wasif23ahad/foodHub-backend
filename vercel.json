{
    "version": 2,
    "installCommand": "pnpm install",
    "buildCommand": "pnpm db:generate",
    "builds": [
        {
            "src": "src/server.ts",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "src/server.ts"
        }
    ]
}