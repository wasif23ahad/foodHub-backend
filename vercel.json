{
    "version": 2,
    "installCommand": "pnpm install",
    "buildCommand": "pnpm db:generate && pnpm build",
    "builds": [
        {
            "src": "dist/server.js",
            "use": "@vercel/node"
        }
    ],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "dist/server.js"
        }
    ]
}