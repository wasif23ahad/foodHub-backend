{
    "version": 2,
    "framework": null,
    "installCommand": "pnpm install && pnpm db:generate",
    "buildCommand": "pnpm build",
    "outputDirectory": "dist",
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/server.js"
        }
    ]
}